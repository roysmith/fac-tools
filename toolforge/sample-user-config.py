# Sample pywikibot user-config.py
# Make a copy of this, ensure that it is mode 0600,
# and insert the appropriate OAuth credentials.  Do
# NOT check these credentials into version control

usernames["wikipedia"]["en"] = "FACSummaryBot"
authenticate["en.wikipedia.org"] = (
    "xxxx",  # consumer_key
    "xxxx",  # consumer_secret
    "xxxx",  # access_key
    "xxxx",  # access_secret
)
